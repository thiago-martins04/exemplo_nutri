// Serviço de IA removido conforme solicitado.
// Este arquivo é mantido vazio ou com exportação nula para evitar erros de importação em arquivos legados.

export const generateMealSuggestion = async (preference: string) => {
  return { suggestion: "Recurso desativado." };
};